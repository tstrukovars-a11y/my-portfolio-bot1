# Версии закреплены точно, и это не придирчивость.
#
# Без номера Render на каждой сборке ставит свежайший выпуск каждой
# библиотеки. Значит, бот может перестать работать в день, когда мы к
# нему вообще не прикасались, — от чужого релиза. Поднимать версии надо
# осознанно: поменяли номер, прогнали тесты, выкатили.
#
# Ниже — ровно те версии, на которых бот работает сейчас.

aiogram==3.28.2
anthropic==0.102.0
httpx==0.28.1
asyncpg==0.31.0
python-dotenv==1.2.2
feedparser==6.0.14
